# 🤖 PersonaForge

> **Telegram-бот с человеческим характером** — создавайте уникальные AI-персоны с долгосрочной памятью, голосом и зрением

[![Rust](https://img.shields.io/badge/Rust-1.78+-orange?logo=rust)](https://www.rust-lang.org/)
[![Telegram](https://img.shields.io/badge/Telegram-Bot%20API-blue?logo=telegram)](https://core.telegram.org/bots/api)
[![Ollama](https://img.shields.io/badge/Ollama-Local%20LLM-green)](https://ollama.com/)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?logo=docker)](https://www.docker.com/)

---

## ✨ Возможности

### 🎭 Персоны
Создавайте уникальные AI-личности с настраиваемыми промптами. Переключайтесь между персонами на лету.

### 🧠 RAG-память с Time-Decay
Бот помнит контекст разговора и использует векторный поиск с учётом давности — свежие воспоминания важнее старых.

### 📝 Автоматическая суммаризация
Старые разговоры автоматически сжимаются в краткие саммари для долгосрочного контекста без потери памяти.

### 🌐 Веб-поиск
Бот знает текущую дату и может искать актуальную информацию через DuckDuckGo.

### 🎤 Голосовые сообщения
Распознавание речи через Whisper API — отправьте голосовое, получите ответ.

### 👁️ Зрение (Vision)
Анализ изображений через мультимодальные модели. Отправьте фото — получите описание.

### 👻 Режим Призрака
Владелец может временно "стать" ботом — отправлять сообщения от его имени и обучать на своих примерах.

### ⚡ Умная очередь
Защита от перегрузки Ollama с настраиваемыми лимитами и таймаутами.

### 🎯 Триггеры
Настройте ключевые слова, на которые бот будет реагировать автоматически.

---

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/your-username/PersonaForge.git
cd PersonaForge
```

### 2. Настройте окружение
```bash
cp .env.example .env
# Отредактируйте .env — добавьте токен бота и ваш Telegram ID
```

### 3. Запустите Ollama
```bash
ollama pull gemma2:2b        # или любая другая модель
ollama pull nomic-embed-text  # для эмбеддингов
ollama pull llava            # для vision (опционально)
```

### 4. Запустите бота
```bash
# С Docker (рекомендуется)
docker-compose up --build

# Или локально
cargo run --release
```

---

## 🎮 Команды

| Команда | Описание |
|---------|----------|
| `/menu` | Главное меню с кнопками |
| `/help` | Список всех команд |
| `/status` | Статус сервисов и очереди |

### 👤 Персоны
```
/create_persona Джарвис|Ты умный ассистент Тони Старка
/list_personas
/activate_persona 1
/update_persona 1|Новое имя|Новый промпт
/delete_persona 1
/export_persona 1
/export_all_personas
/import_persona (+ JSON файл)
```

### 👻 Режим Призрака
```
/ghost on       — включить (с сохранением примеров)
/ghost on nosave — включить (без сохранения)
/ghost off      — выключить
/ghost status   — проверить статус
```

### 🎯 Триггеры
```
/triggers помощь, вопрос, подскажи
/triggers clear
```

### ⚙️ Настройки
```
/enable_rag / /disable_rag
/set_memory_depth 20
/reply_to_all / /reply_to_mention
/set_cooldown 10
```

### 📢 Админ
```
/broadcast Привет всем!
/stats
/models
```

---

## ⚙️ Конфигурация

### Основные
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELOXIDE_TOKEN` | Токен бота от @BotFather | — |
| `OWNER_ID` | Ваш Telegram User ID | — |
| `DATABASE_URL` | Путь к SQLite | `sqlite:persona_forge.db` |
| `OLLAMA_URL` | URL Ollama API | `http://host.docker.internal:11434` |
| `OLLAMA_CHAT_MODEL` | Модель для чата | `gemma2:2b` |
| `OLLAMA_EMBEDDING_MODEL` | Модель для эмбеддингов | `nomic-embed-text` |

### Очередь и производительность
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `MAX_CONCURRENT_LLM_REQUESTS` | Лимит параллельных запросов | `3` |
| `LLM_TIMEOUT_SECONDS` | Таймаут генерации | `120` |
| `QUEUE_TIMEOUT_SECONDS` | Таймаут ожидания в очереди | `30` |

### Фичи
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VISION_ENABLED` | Анализ изображений | `false` |
| `OLLAMA_VISION_MODEL` | Модель для vision | `llava:latest` |
| `VOICE_ENABLED` | Распознавание голоса | `false` |
| `WHISPER_URL` | URL Whisper API | `http://localhost:8080` |
| `WEB_SEARCH_ENABLED` | Веб-поиск | `true` |

### RAG и память
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `RAG_DECAY_RATE` | Скорость затухания памяти (0.0-1.0) | `0.1` |
| `SUMMARY_THRESHOLD` | Сообщений до авто-суммаризации | `50` |
| `RANDOM_REPLY_PROBABILITY` | Вероятность случайного ответа | `0.0` |

---

## 🏗️ Архитектура

```
PersonaForge/
├── src/
│   ├── bot/handlers/     # Команды, сообщения, callback'ы
│   ├── db/               # SQLx (персоны, настройки, RAG, саммари)
│   ├── llm/              # Ollama клиент (генерация, эмбеддинги, vision)
│   ├── voice/            # Whisper клиент (STT)
│   ├── web/              # DuckDuckGo поиск
│   ├── config.rs         # Конфигурация из ENV
│   └── state.rs          # Shared state
├── migrations/           # SQL миграции
└── docker-compose.yml
```

### Ключевые компоненты

- **Thread Support** — ответы в тредах супергрупп
- **Persona Export/Import** — JSON формат для переноса персон
- **Time-Decay RAG** — экспоненциальное затухание релевантности старых воспоминаний
- **Auto-Summarization** — фоновая суммаризация при накоплении сообщений
- **WizardState** — пошаговые диалоги для создания персон
- **GhostMode** — режим "призрака" для владельца
- **LLM Semaphore** — очередь с ограничением параллельных запросов
- **WebSearchClient** — поиск актуальной информации

---

## 🐳 Docker

```yaml
services:
  bot:
    build: .
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
```

Бот подключается к Ollama на хост-машине через `host.docker.internal`.

---

## 📊 Мониторинг

Команда `/status` показывает:
- Состояние Ollama и БД
- Активную персону
- Статус режима призрака
- Очередь LLM (свободные слоты, статистика)

Команда `/stats` — детальная статистика очереди и времени ответа.

---

## 🛣️ Roadmap

- [x] Интерактивные меню (Inline Keyboards)
- [x] Wizard для создания персон
- [x] Режим призрака (Ghost Mode)
- [x] Очередь LLM с лимитами
- [x] Vision (анализ изображений)
- [x] Keyword triggers
- [x] Broadcast рассылка
- [x] Web Search (DuckDuckGo)
- [x] Voice (распознавание голоса)
- [x] Суммаризация памяти
- [x] Time-decay для RAG
- [x] Поддержка тредов в супергруппах
- [x] Экспорт/импорт персон
- [ ] Web UI для управления

---

## 📝 Лицензия

MIT

---

<p align="center">
  <b>Сделано с ❤️ и 🦀</b>
</p>

# 📱 Mini App (Веб-панель)

Mini App — это веб-панель управления для владельца бота, встроенная прямо в Telegram.

## Возможности

| Вкладка | Функции |
|---------|---------|
| 📊 Status | Мониторинг Ollama, БД, очереди LLM |
| 🎭 Personas | Создание, редактирование, удаление, активация |
| 💬 Chats | Настройки всех чатов |
| 🛡️ Security | Блокировка пользователей, статистика |
| ⚙️ Config | Модель, температура, токены, тогглы |

## Настройка

### 1. Запусти туннель (для HTTPS)

Mini App требует HTTPS. Варианты:

**serveo.net** (бесплатно, без регистрации):
```bash
ssh -R 80:localhost:8080 serveo.net
```

**ngrok** (нужна регистрация):
```bash
ngrok http 8080
```

**cloudflared**:
```bash
cloudflared tunnel --url http://localhost:8080
```

### 2. Создай Mini App в BotFather

1. Открой [@BotFather](https://t.me/BotFather)
2. Отправь `/newapp`
3. Выбери своего бота
4. Заполни:
   - **Название**: PersonaForge Panel
   - **Описание**: Owner control panel
   - **URL**: `https://your-tunnel-url.com`

### 3. Добавь кнопку меню

1. В BotFather отправь `/setmenubutton`
2. Выбери бота
3. Выбери тип: `web_app`
4. Текст кнопки: `🎛️ Panel`
5. URL: `https://your-tunnel-url.com`

## Аутентификация

Mini App использует Telegram WebApp authentication:

1. Telegram передаёт `initData` с подписью
2. Бот проверяет подпись через HMAC-SHA256
3. Проверяется `user_id == OWNER_ID`
4. Доступ только для владельца

## Интерфейс

### Status

```
┌─────────────────────────────────────┐
│  🟢 Ollama: Connected               │
│  🟢 Database: OK                    │
│  📊 Queue: 0/3                      │
│  ⏱️  Uptime: 2h 34m                 │
└─────────────────────────────────────┘
```

### Personas

- Список всех персон с превью промпта
- Кнопки: Edit, Activate, Delete
- Форма создания новой персоны
- Поля: name, display_name, prompt, triggers

### Chats

- Список всех чатов где есть бот
- Настройки для каждого чата:
  - Активная персона
  - RAG вкл/выкл
  - Триггеры
  - Режим ответов

### Config

- Выбор модели (dropdown)
- Слайдер температуры
- Поле max_tokens
- Тогглы: Vision, Voice, Web Search
- Кнопка Pause/Resume

## Runtime Config

Настройки из Mini App сохраняются в БД и применяются без перезапуска:

| Параметр | Описание |
|----------|----------|
| `ollama_chat_model` | Модель для чата |
| `temperature` | Температура генерации |
| `max_tokens` | Максимум токенов |
| `vision_enabled` | Анализ изображений |
| `voice_enabled` | Голосовые сообщения |
| `web_search_enabled` | Веб-поиск |

---

➡️ Далее: [[Voice|Голосовые сообщения]]

# 🌐 Веб-поиск

Puppeteer может искать актуальную информацию в интернете через DuckDuckGo.

## ✅ Реализовано в Puppeteer v1.0.0

Система веб-поиска полностью интегрирована:

- **LLM-роутинг**: AI определяет нужен ли поиск (`should_search()`)
- **DuckDuckGo HTML Scraping**: Парсинг топ-3 результатов через `scraper` crate
- **Автогенерация запросов**: LLM формирует оптимальный поисковый запрос
- **Инъекция контекста**: `[Ты только что загуглил это и прочитал...]`
- **Гуманизация**: Дополнительные задержки при поиске (симуляция гугления)
- **Privacy-focused**: Без трекинга, без API ключей

## Как это работает

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Вопрос    │ ──▶ │ DuckDuckGo  │ ──▶ │ Результаты  │
└─────────────┘     └─────────────┘     └─────────────┘
                                               │
                                               ▼
                                        ┌─────────────┐
                                        │     LLM     │
                                        │  + контекст │
                                        └─────────────┘
```

1. LLM определяет, что нужен веб-поиск
2. Формирует поисковый запрос
3. DuckDuckGo возвращает результаты
4. Результаты добавляются в контекст
5. LLM генерирует ответ с актуальной информацией

## Настройка

### Текущая реализация (v1.0.0)

Веб-поиск работает автоматически для всех аккаунтов. Никакой настройки не требуется!

**Технические детали:**
- Поисковик: DuckDuckGo HTML (`https://html.duckduckgo.com/html/`)
- Парсинг: CSS селекторы через `scraper` crate
- Результаты: Топ-3 (title, snippet, url)
- Детекция: LLM с temperature=0.1 для точности
- User-Agent: `Mozilla/5.0` для обхода блокировок

### Будущие настройки

```env
WEB_SEARCH_ENABLED=true  # Планируется
```

Никаких API ключей не нужно — DuckDuckGo работает без регистрации.

## Когда используется

Бот автоматически ищет в интернете когда:

- Вопрос о текущих событиях
- Запрос актуальной информации (цены, курсы, погода)
- Вопрос о чём-то, чего нет в знаниях модели
- Явная просьба "загугли", "найди в интернете"

## Примеры

```
Пользователь: Какая сейчас погода в Москве?
Бот: [ищет в DuckDuckGo] → Сейчас в Москве -5°C, облачно...

Пользователь: Последние новости про Rust
Бот: [ищет] → Вышел Rust 1.75 с новыми фичами...

Пользователь: Сколько стоит биткоин?
Бот: [ищет] → Текущий курс BTC: $43,000...
```

## Ограничения

- Результаты зависят от DuckDuckGo
- Не все сайты индексируются
- Может быть задержка в актуальности
- Некоторые запросы могут не дать результатов

## Приватность

- Запросы идут через DuckDuckGo (privacy-focused)
- IP пользователя не передаётся
- История поиска не сохраняется на серверах DuckDuckGo

## Troubleshooting

### Поиск не работает

1. Проверь `WEB_SEARCH_ENABLED=true`
2. Проверь интернет-соединение
3. DuckDuckGo может быть временно недоступен

### Нерелевантные результаты

- Переформулируй вопрос
- Будь конкретнее
- Укажи контекст (год, страна, область)

### Бот не ищет когда нужно

Явно попроси:
- "Загугли..."
- "Найди в интернете..."
- "Какие последние новости о..."

---

➡️ Далее: [[Security|Безопасность]]
